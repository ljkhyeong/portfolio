# 포트폴리오 작업 지침

## 작업 기준

- 설명, 주석, 문서와 PR 리뷰는 한글로 작성하고 실무 용어로 짧고 명확하게 표현한다. 작업이 끝나면 변경을 분류해 `feat:`, `fix:`, `docs:` 등의 접두어와 한글 제목으로 커밋한다.
- 프로젝트의 역할, 수치와 성과는 현재 포트폴리오와 확인한 자료에 근거한다. 구현, 로컬 검증, 실제 운영 결과를 구분한다.
- 기존 코드와 `.prettierrc`를 따르고, 포맷 변경은 수정한 파일에 한정한다.

## 수정 위치

- 웹은 React와 Vite를 사용한다. 화면은 `src/component/`, 스타일은 `src/css/`, 경로는 `src/App.jsx`에서 관리한다.
- 프로필과 프로젝트 설명은 `src/data/`가 기준이다. 메인 요약과 상세 설명을 함께 확인하고, 같은 값을 화면별로 하드코딩하지 않는다.
- 프로젝트 상세는 `src/component/project/ProjectCaseStudy.jsx` 등 공용 컴포넌트를 우선 사용한다. 경로를 추가하면 `src/App.jsx`와 `src/data/routeMeta.js`를 함께 갱신한다.
- 검색 API는 `knowledge-api/`의 Spring Boot 애플리케이션이다. 실행과 검증은 `knowledge-api/README.md`를 따른다. 공개 검색 자료에 회사 비공개 자료나 자기소개서 초안을 포함하지 않는다.

## 실행과 검증

- Node 버전은 `.nvmrc`, 실행 명령은 `package.json`이 기준이다. 개발 서버는 `npm run dev`로 실행한다.
- 동작 변경은 관련 Vitest 테스트로 확인하고, 배포에 영향을 주는 변경은 `npm run build`로 확인한다. 화면 변경은 해당 화면의 데스크톱과 모바일 표시를 확인한다.
- 단순 문구나 문서 수정에는 별도 테스트를 만들지 않는다. 문서만 바꾸면 경로, 명령과 diff를 확인한다. 새 변경이나 실패 원인이 없으면 통과한 검증을 반복하지 않는다.
- 빌드는 검색 자료를 생성하고 sitemap, OG 이미지와 PDF의 최신 여부를 검사한다. `scripts/artifact-inputs.mjs`와 검사 결과를 기준으로 영향받은 산출물을 다시 생성한다. 생성 명령과 상세 절차는 `README.md`를 참고한다.
- 배포 PDF는 `public/임정규_포트폴리오.pdf`이다. 갱신하면 다운로드 링크와 인쇄 결과도 확인한다.

## 스킬과 글쓰기

- 스킬은 실제 작업 범위에 맞을 때 적용한다. 포트폴리오에 소개된 프로젝트 이름만으로 다른 저장소 전용 스킬을 적용하지 않는다.
- UI는 경력과 구현 내용을 빠르게 읽을 수 있게 구성한다. 디자인 스킬의 이미지, 모션과 레이아웃 권장안은 이 목적에 맞을 때 적용한다. 문구 수정에 전체 디자인 재설계 절차를 적용하지 않는다.
- 자기소개서나 지원 답변을 작성할 때는 `self-intro/00_workflow.txt`를 읽는다.
